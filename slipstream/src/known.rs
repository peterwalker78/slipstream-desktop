//! The screens Slipstream has seen before, and what each one comes back to.
//!
//! The types live in `slipstream-config` so the Settings app reads the same list the compositor
//! writes; this is the compositor's name for them.

pub use slipstream_config::screens::*;
